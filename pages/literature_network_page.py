import streamlit as st


def render_literature_network_page() -> None:
    st.divider()
    st.markdown("# Literature Network")
    st.info(
        "This tab is currently being developed. "
        "It will show the network of relevant publications."
    )
